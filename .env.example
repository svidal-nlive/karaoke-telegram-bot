# -----------------------------------------------------------------------------
# Karaoke MVP - Example .env Configuration
# Copy this file to .env and fill in the required values for your deployment.
# -----------------------------------------------------------------------------

DEBUG=true
# or
# ENV=dev

# UID and GID to run services as (default: 1000)
# Used by Dockerfiles to set the container's internal user.
PUID=1001
PGID=1001

# Optional: fallback defaults used *inside containers* if not defined
# These should only be overridden if you're debugging
DEFAULT_PUID=1000
DEFAULT_PGID=1000

# Timezone
TZ=UTC

# Stack Prefix for naming containers and volumes
STACK_PREFIX=karaoke

# Network name for all services (optional)
BACKEND_NETWORK=backend

# Telegram bot config
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_primary_chat_id_here

# Optional: YouTube cookies support (bind-mounted from host)
YT_DLP_COOKIES_DIR=./cookies
YT_DLP_COOKIES_FILE=/cookies/cookies.txt

# Grafana
GRAFANA_ADMIN_PASSWORD=admin

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Cron Schedule for cleanup service
CLEANUP_CRON_SCHEDULE=0 3 * * *

# Optional: external domain if using tunnels/proxies
DOMAIN=vectorhost.net
TUNNEL_TOKEN=

# --- Splitter Service Options ---
CHUNKING_ENABLED=false   # (true/false) If false, disables chunking and processes whole track at once
CHUNK_LENGTH_MS=240000   # Only used if chunking is enabled
SPLITTER_TYPE=SPLEETER   # SPLEETER or DEMUCS
STEMS=2                  # Number of stems: 2, 4, or 5 (supported by chosen backend)
STEM_TYPE=accompaniment  # Comma-separated list of stem names to output (auto-filters unsupported)
